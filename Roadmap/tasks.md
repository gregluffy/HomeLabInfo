# Disclosures
*   This app is not trying at least for now to replace some well known and established open source tools like (Grafana, Portainer, Beszel etc).

# Done
* Inform the user in the footer that update is available. And add the github link to the footer.
* Add total Ram and total Disk space Virtual Machine cards.
* When multiple VM added with containers the nodes of the Containers overlap each other on the Topology.
* The position of the nodes are not saved properly. Also we could save the last Zoom selection the User did.
* Security improvement on the Docker netword host access.
* Responsivenes improvements for mobile device.

# Todo
* Add and ability to add notes on the Topology view. 
* Add the ability to group some nodes in topology view. maybe the user would group them like IOT/Servers etc..
* Also in general think about how to improve the Topology View cause if we have a lot of nodes its kinda messy especially with normal monitors.
* Request for multiple Subnets scan.
* Bug: an user sayd that in his network the app assumed the default router wrong and he cannot change it in topology.

# Backlog
* Maybe an login functionality.
* Maybe export the topology as json to give the ability to have the topology as actual data.